/*
#include <iostream>
#include <set>
using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int N; cin >> N;
	int data;
	set<int> s;
	for (int i = 0; i < N; i++) {
		cin >> data;
		s.insert(data);
	}
	for (auto& i : s) { cout << i << '\n'; }
}
*/